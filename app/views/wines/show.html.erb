<% content_for :name, @wine.name %>

<!--
Removed the following from class 10, lab 5, Applying Layouts
<%= render 'shared/flashes' %>
-->

<article class="wine">
  <header>
  <h2><%= @wine.name %></h2>
  </header>
  <p><label>Year:  </label><%= @wine.year %></p>

  <!-- 
  Removed thousands comma delimiter "number_with_delimiter(@wine.year)"
  number_with_delimiter(@wine.year)
  -->

  <p><label>Winery:  </label><%= @wine.winery %></p>
  <p><label>Country:  </label><%= @wine.country %></p>
  <p><label>Varietal:  </label><%= @wine.varietal %></p>
  <p><label>Average Log Entry:</label> <%= format_average_rating(@wine) %></p>
 </article>
 
 <%= link_to 'All Wines', wines_path %>
 | <%= link_to 'Edit', edit_wine_path(@wine) %>
<%= button_to 'Delete', @wine,
			   method: :delete,
			   data: { confirm:'Are you sure?' } %>

<p>
  Read all
  <%= link_to pluralize(@wine.log_entries.size, 'log_entry'), 
                        wine_log_entries_path(@wine) %>
</p>